//判断一个数字x二进制下第i位是不是等于1
return ((1 << ( i - 1 )) & x) > 0;

//将一个数字x二进制下第i位更改成1
x = x | (1 << (i - 1));

//把一个数字二进制下最靠右的第一个1去掉
x = x & (x - 1);

//求数字中1的个数(循环次数只和1的出现次数有关，原理是每次去掉最低位的1)
int cnt = 0; while (x) x &= x - 1, cnt++;

// 枚举状态S的所有子集
for (int x = S; x; x = (x-1)&S)
